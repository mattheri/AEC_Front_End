import React from 'react';
import Card from "react-bootstrap/Card";

export const Personnages = (props) => {
    
    console.log(props);

    return (
        <section className="Personnages">
            
        </section>
    )
}
