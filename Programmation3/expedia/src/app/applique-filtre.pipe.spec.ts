import { AppliqueFiltrePipe } from './applique-filtre.pipe';

describe('AppliqueFiltrePipe', () => {
  it('create an instance', () => {
    const pipe = new AppliqueFiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
